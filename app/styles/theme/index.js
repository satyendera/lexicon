import colors from './colors';
import zIndex from './zIndex';
import breakpoints from './breakpoints';


const theme = {
  primaryColor: colors.WHITE_SMOKE,
};

const themes = { zIndex, theme,breakpoints};
export default themes;
